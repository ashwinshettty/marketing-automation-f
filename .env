# Same keys as Amplify — only hosts differ locally
# Amplify: VITE_MARKETING_AUTOMATION_API_URL=https://api.bot.inkstall.in/api
# Amplify: VITE_WI_API_URL=https://api.bot.inkstall.in

# marketing-automation-b (port 3000) — auth, WhatsApp, events, leads
VITE_MARKETING_AUTOMATION_API_URL=http://localhost:3000/api

# Website Intelligence API (same MA-b server, no /api suffix — client adds /api/...)
VITE_WI_API_URL=http://localhost:3000
